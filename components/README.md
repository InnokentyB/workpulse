# Components

The P0 UI will be split into `ScenarioSelector`, `WorkContextCard`, `DecisionCard`, `ActivityCard`, `ActivitySession`, and `InterventionHistory` after the decision contract is covered by tests.
